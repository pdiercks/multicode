# dolfin.parameters for krylov solver
krylov_solver:
    relative_tolerance: 1.0e-2
    absolute_tolerance: 1.0e-12
    maximum_iterations: 1000
# solver parameters dolfin.solve()
solver_parameters:
    linear_solver: "default"
    preconditioner: "default"
